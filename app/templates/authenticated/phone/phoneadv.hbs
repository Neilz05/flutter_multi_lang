{{page-title (t "SUB_NAVIGATION_ITEM_PHONEADV")}}

<div class="card mb-3">
  <div class="card-header sc">
    <label class="fs-4 fw-bold ms-3" tabindex="0" aria-label="{{t 'PAGE_PHONEADV_VOICE_DIAL_PLAN'}}">{{t 'PAGE_PHONEADV_VOICE_DIAL_PLAN'}}</label>
  </div>
  <div class="card-body">
    {{#if (not-eq this.voipDailTimeout null)}}
      {{#let this.voipDailTimeout as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.firstDigitTimeout null)}}
      {{#let this.firstDigitTimeout as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.minHookFlash null)}}
      {{#let this.minHookFlash as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.maxHookFlash null)}}
      {{#let this.maxHookFlash as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}
  </div>
</div>
<div class="card mb-3">
  <div class="card-header sc">
    <label class="fs-4 fw-bold ms-3" tabindex="0" aria-label="{{t 'PAGE_PHONEADV_VOICEAUDIOCODEC'}}">{{t 'PAGE_PHONEADV_VOICEAUDIOCODEC'}}</label>
  </div>
  <div class="card-body">
    {{#if (not-eq this.preferedCodec1 null)}}
      {{#let this.preferedCodec1 as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <select class="form-select" {{on "change" (fn cl.onUpdate cl)}}>
                  {{#each this.arAudioCodecOptions as |server|}}
                    <option value={{server}} selected={{eq server cl.value}}>{{server}}</option>
                  {{/each}}
              </select>
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.preferedCodec1_packetization null)}}
      {{#let this.preferedCodec1_packetization as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <select class="form-select" {{on "change" (fn cl.onUpdate cl)}}>
                  {{#each this.arAudioCodecPacketizationOptions as |server|}}
                    <option value={{server}} selected={{eq server cl.value}}>{{server}}</option>
                  {{/each}}
              </select>
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.preferedCodec1_state null)}}
      {{#let this.preferedCodec1_state as |cl|}}
			 {{#let this.preferedCodec1_state_onoffvalue as |onoffvalue|}}
				<div class="row py-1 ps-3">
					<div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
					<div class="col">
            <label class="switch" for="{{cl.buttonId}}" tabindex="0" aria-label="{{t cl.title}}" role="button" {{on "keyup" this.eaactrl.onoffTrigger}}>
              <Input id="{{cl.buttonId}}" tabindex="-1" @type="checkbox" @value={{onoffvalue}} @checked={{onoffvalue}} class="form-check-input" />
              <span class="slider round"></span>
            </label>
					</div>
				</div>
			 {{/let}}
			{{/let}}
    {{/if}}
    {{#if (not-eq this.preferedCodec2 null)}}
      {{#let this.preferedCodec2 as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <select class="form-select" {{on "change" (fn cl.onUpdate cl)}}>
                  {{#each this.arAudioCodecOptions as |server|}}
                    <option value={{server}} selected={{eq server cl.value}}>{{server}}</option>
                  {{/each}}
              </select>
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.preferedCodec2_packetization null)}}
      {{#let this.preferedCodec2_packetization as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <select class="form-select" {{on "change" (fn cl.onUpdate cl)}}>
                  {{#each this.arAudioCodecPacketizationOptions as |server|}}
                    <option value={{server}} selected={{eq server cl.value}}>{{server}}</option>
                  {{/each}}
              </select>
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.preferedCodec2_state null)}}
      {{#let this.preferedCodec2_state as |cl|}}
			 {{#let this.preferedCodec2_state_onoffvalue as |onoffvalue|}}
				<div class="row py-1 ps-3">
					<div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
					<div class="col">
            <label class="switch" for="{{cl.buttonId}}" tabindex="0" aria-label="{{t cl.title}}" role="button" {{on "keyup" this.eaactrl.onoffTrigger}}>
              <Input id="{{cl.buttonId}}" tabindex="-1" @type="checkbox" @value={{onoffvalue}} @checked={{onoffvalue}} class="form-check-input" />
              <span class="slider round"></span>
            </label>
					</div>
				</div>
			 {{/let}}
			{{/let}}
    {{/if}} 

		
    <div class="row py-1 ps-3">
      <div class="col" tabindex="0" aria-label="{{t 'PAGE_PHONEADV_T38'}}"><label>{{t 'PAGE_PHONEADV_T38'}}</label></div>
      <div class="col">
            <label class="switch" for="T38Enable" tabindex="0" aria-label="{{t 'PAGE_PHONEADV_T38'}}" role="button" {{on "keyup" this.eaactrl.onoffTrigger}}>
              <Input id="T38Enable" tabindex="-1" @type="checkbox" @value={{this.T38Enable}} @checked={{this.T38Enable}} class="form-check-input" />
              <span class="slider round"></span>
            </label>
      </div>
    </div>
		
  </div>
</div>

<div class="card mb-3">
  <div class="card-header sc">
    <label class="fs-4 fw-bold ms-3" tabindex="0" aria-label="{{t 'PAGE_PHONEADV_OTHERSETTINGS'}}">{{t 'PAGE_PHONEADV_OTHERSETTINGS'}}</label>
  </div>
  <div class="card-body">
    {{#if (not-eq this.location null)}}
      {{#let this.location as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <select class="form-select" {{on "change" (fn cl.onUpdate cl)}}>
                  {{#each this.arLocationOptions as |server|}}
                    <option value={{server}} selected={{eq server cl.value}}>{{server}}</option>
                  {{/each}}
              </select>
            </div>
        </div>
			{{/let}}
    {{/if}}

    {{#if (not-eq this.echoCancellation null)}}
      {{#let this.echoCancellation as |cl|}}
			 {{#let this.echoCancellation_onoffvalue as |onoffvalue|}}
				<div class="row py-1 ps-3">
					<div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
					<div class="col">
            <label class="switch" for="{{cl.buttonId}}" tabindex="0" aria-label="{{t cl.title}}" role="button" {{on "keyup" this.eaactrl.onoffTrigger}}>
              <Input id="{{cl.buttonId}}" tabindex="-1" @type="checkbox" @value={{onoffvalue}} @checked={{onoffvalue}} class="form-check-input" />
              <span class="slider round"></span>
            </label>
					</div>
				</div>
			 {{/let}}
			{{/let}}
    {{/if}} 

    {{#if (not-eq this.vadSupport null)}}
      {{#let this.vadSupport as |cl|}}
			 {{#let this.vadSupport_onoffvalue as |onoffvalue|}}
				<div class="row py-1 ps-3">
					<div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
					<div class="col">
            <label class="switch" for="{{cl.buttonId}}" tabindex="0" aria-label="{{t cl.title}}" role="button" {{on "keyup" this.eaactrl.onoffTrigger}}>
              <Input id="{{cl.buttonId}}" tabindex="-1" @type="checkbox" @value={{onoffvalue}} @checked={{onoffvalue}} class="form-check-input" />
              <span class="slider round"></span>
            </label>
					</div>
				</div>
			 {{/let}}
			{{/let}}
    {{/if}} 
		
    {{#if (not-eq this.dscpForRtp null)}}
      {{#let this.dscpForRtp as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}
    {{#if (not-eq this.signallingDscp null)}}
      {{#let this.signallingDscp as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}		
    {{#if (not-eq this.localRtpMaxPort null)}}
      {{#let this.localRtpMaxPort as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}				
    {{#if (not-eq this.localRtpMinPort null)}}
      {{#let this.localRtpMinPort as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}				
    {{#let this.rtcpPacketInterval as |cl|}}
      {{#if (not-eq cl null)}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
      {{/if}}				
		{{/let}}
		
    {{#if (not-eq this.registrationExpireTimeout null)}}
      {{#let this.registrationExpireTimeout as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}				
    {{#if (not-eq this.registrationRetryInterval null)}}
      {{#let this.registrationRetryInterval as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}				
    {{#if (not-eq this.primaryProxyRetryInterval null)}}
      {{#let this.primaryProxyRetryInterval as |cl|}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
			{{/let}}
    {{/if}}				
		
	  {{#let this.sessionTimerExpires as |cl|}}
      {{#if (not-eq cl null)}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
      {{/if}}				
		{{/let}}	
    {{#let this.sessionTimerMinSe as |cl|}}
      {{#if (not-eq cl null)}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <Ui::InputWithErrormsg @value={{cl.value}} @disabled={{false}} @obj={{cl}} @onUpdate={{(fn cl.onUpdate)}} @errorMessage="{{cl.error}}" />
            </div>
        </div>
      {{/if}}				
		{{/let}}		
    {{#let this.prack as |cl|}}
      {{#if (not-eq cl null)}}
			 {{#let this.prack_onoffvalue as |onoffvalue|}}
				<div class="row py-1 ps-3">
					<div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
					<div class="col">
            <label class="switch" for="{{cl.buttonId}}" tabindex="0" aria-label="{{t cl.title}}" role="button" {{on "keyup" this.eaactrl.onoffTrigger}}>
              <Input id="{{cl.buttonId}}" tabindex="-1" @type="checkbox" @value={{onoffvalue}} @checked={{onoffvalue}} class="form-check-input" />
              <span class="slider round"></span>
            </label>
					</div>
				</div>
			 {{/let}}
      {{/if}}				
		{{/let}}				
		
    {{#let this.interfaceName as |cl|}}
      {{#if (not-eq cl null)}}
        <div class="row py-1 ps-3">
            <div class="col" tabindex="0" aria-label="{{t cl.title}}"><label>{{t cl.title}}</label></div>
            <div class="col">
              <select class="form-select" {{on "change" (fn cl.onUpdate cl)}}>
                  {{#each this.arOptions_interfaceName as |server|}}
                    <option value={{server.value}} selected={{eq server.value cl.value}}>{{server.display}}</option>
                  {{/each}}
              </select>
            </div>
        </div>
      {{/if}}				
		{{/let}}		

  </div>
</div>
{{!--
        <div class="errMsg alert alert-danger" role="alert" tabindex="0" aria-label="error">
            error
        </div>
--}}
<div class="d-flex justify-content-end">
  <button type="button" class="btn btn-primary" style="width: fit-content;" {{on "click" this.applyPage}} disabled={{unless this.hasChanges "disabled"}}>{{t 'PAGE_SETTINGS_APPLY'}}</button>
  <button type="button" class="btn btn-secondary ms-2" style="width: fit-content;" onClick={{this.cancelPage}}>{{t 'PAGE_SETTINGS_CANCEL'}}</button>
</div>