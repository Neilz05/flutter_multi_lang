{{page-title "Phone"}}
<h2 tabindex="0" aria-label="{{t 'NAVIGATION_ITEM_PHONE'}}">{{t 'NAVIGATION_ITEM_PHONE'}}</h2>


{{!-- <ul class="nav nav-pills">
  <li class="nav-item">
    <a class="nav-link {{if this.phonesettingsTabActive "active"}}" {{on 'click' (fn this.setActiveTab 'phonesettings')}}>{{t 'PAGE_PHONE_SETTINGS_TITLE'}}</a>
  </li>
  <li class="nav-item">
    <a class="nav-link {{if this.voicestatusTabActive "active"}}" {{on 'click' (fn this.setActiveTab 'voicestatus')}}>{{t 'PAGE_VOICE_STATUS_TITLE'}}</a> 
  </li>
</ul> --}}

{{!-- {{#if this.phonesettingsTabActive}} --}}
  {{!-- {{#each @model.VoiceService as |voiceservice|}}
    <Phone::PhoneSettings @sip={{voiceservice.SIP}} />
  {{/each}} --}}


{{log @model.network}}
{{log @model.network2}}
{{log @model.clients}}

<Phone::PhoneSettings
  @network={{@model.network}}
  @network2={{@model.network2}}
  @clients={{@model.clients}}
/>

  {{!-- <button type="button" class="btn btn-primary" {{on "click" this.updateVoice}} disabled={{unless this.hasChanges "disabled"}}>{{t 'UPDATE'}}</button> --}}

<div class="d-flex justify-content-end">
  <button type="button" class="btn btn-primary" style="width: fit-content;" {{on "click" this.updateVoice}} disabled={{unless this.hasChanges "disabled"}}>
    {{t 'PAGE_SETTINGS_APPLY'}}
  </button>
  <button type="button" class="btn btn-secondary ms-2" style="width: fit-content;" onClick={{this.cancelPhone}}>
    {{t 'PAGE_SETTINGS_CANCEL'}}
  </button>
</div>

{{!-- {{/if}} --}}

{{!-- {{#if this.voicestatusTabActive}}
    <Phone::VoiceStatus @voicestatus = {{@model}}/>
{{/if}} --}}

