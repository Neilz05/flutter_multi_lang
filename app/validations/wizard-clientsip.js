import { validatePresence } from 'ember-changeset-validations/validators';

export default {
    /* "X_PRPLWARE-COM_DisplayName": validatePresence({ presence: true, message: 'Display Name is required.' }),
    RegisterURI: validatePresence({ presence: true, message: 'Registrar URI is required.' }),
    AuthUserName: validatePresence({ presence: true, message: 'Username is required.' }),
    AuthPassword: validatePresence({ presence: true, message: 'Password Port is required.' }), */
};
